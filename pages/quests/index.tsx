import { StyledContent } from '@/shared/components';
import { StyledQuests } from './quests';

const Home = () => (
    <StyledContent>
        <StyledQuests />
    </StyledContent>
);

export default Home;
